# Required: Slack Bot Token (starts with xoxb-)
# Get this from your Slack app's "OAuth & Permissions" page
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Required: Slack Signing Secret
# Get this from your Slack app's "Basic Information" page
SLACK_SIGNING_SECRET=your-signing-secret-here

# Recommended: Channel ID where incident announcements are posted
# Create a #incidents channel and copy its ID from channel details
TEAM_UPDATE_CHANNEL_ID=C123456789

# Development only: Socket Mode
# For local development, enable socket mode to avoid needing a public URL
# 1. Set SOCKET_MODE=true
# 2. Enable Socket Mode in your Slack app settings
# 3. Generate an App-Level Token with 'connections:write' scope
# 4. Set SLACK_APP_TOKEN below
SOCKET_MODE=false
SLACK_APP_TOKEN=

# Production: HTTP Mode (default)
# For production deployments, use HTTP mode (SOCKET_MODE=false or unset)
# Configure your app's Request URL to point to: https://your-domain.com/slack/events
PORT=3000
